function addition(value: string){//created function addition
    console.log("Value is: " + value);//outputting to console
}

let fisrtVal: number = 42;//assigning var fisrtVal as a number
let secondVal: number = 1;//assigning var secondVal as a number
let sumOfVals: string = (fisrtVal+secondVal).toLocaleString();//created a var called sumOfVal which is previous 2 vars added together and ouputted as a string

addition(sumOfVals);//calling fucntion addition and passing it sumofVals